name: inicio_sesion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  logger: ^2.5.0
  file_picker: ^9.0.1
  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  url_launcher: ^6.1.12
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  image_picker: ^1.1.2
  numberpicker: ^2.1.1

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/

